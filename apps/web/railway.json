{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @df/web build"
  },
  "deploy": {
    "startCommand": "cd apps/web && pnpm start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
