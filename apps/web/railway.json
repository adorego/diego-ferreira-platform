{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npx pnpm@9 install --frozen-lockfile && npx pnpm@9 --filter @df/web build"
  },
  "deploy": {
    "startCommand": "cd apps/web && npx pnpm@9 start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}