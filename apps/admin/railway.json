{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npx pnpm@9 install --frozen-lockfile && npx pnpm@9 --filter @df/admin build"
  },
  "deploy": {
    "startCommand": "cd apps/admin && npx pnpm@9 start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
