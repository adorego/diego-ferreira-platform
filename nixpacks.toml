[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = [
  "corepack disable",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @df/api db:generate && pnpm --filter @df/api build"]

[start]
cmd = "cd apps/api && node dist/src/main"