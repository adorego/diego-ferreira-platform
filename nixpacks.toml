[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm turbo build --filter=@df/api..."]

[start]
cmd = "cd apps/api && node dist/src/main"
